<h1>Animals Index</h1>

<ul>

  <%@animals.each do |animal|%>
    <li> <b><%= link_to animal.name, animal_path(animal)%></b> | <%= link_to "Edit", edit_animal_path(animal)%> | <%=link_to "Delete Animal", animal_path(animal), :data => {:confirm => "Are you sure", :method => "delete"}%></li>
  <%end%>

</ul>

<p>
  <%= link_to 'New Animal', new_animal_path %>
</p>
